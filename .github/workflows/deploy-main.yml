name: Deploy main

on:
  push:
    branches: [main]
    pull_request:
      branches: [main]
      types: [closed]

jobs:
  deploy:
    if: > 
      github.event.pull_request.merged == true &&
      github.event.pull_request.base.ref == 'main' &&
      github.event.pull_request.head.ref == 'dev'

# - name: Deploying to Cloudfront
#        uses: jakejarvis/s3-sync-action@master
#        with:
#          args: --follow-symlinks